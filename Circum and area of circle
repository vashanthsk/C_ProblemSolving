#include <stdio.h>
#include <conio.h>
void main()
{
float pi, r, area, circum;
clrscr();
pi = 3.14;
printf ("Enter radius:");
scanf("%f", &r);
area = pi*r*r;
circum = 2*pi*r;
printf ("Area is: %.2f\n", area);
printf ("Circumference is : %.2f", circum);
getch();
}
